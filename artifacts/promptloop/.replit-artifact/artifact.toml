kind = "web"
previewPath = "/"
title = "PromptLoop"
version = "1.0.0"
id = "artifacts/promptloop"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8080

[services.development]
run = "pnpm --filter @workspace/promptloop run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/promptloop", "run", "build" ]
publicDir = "artifacts/promptloop/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "8080"
BASE_PATH = "/"
